<p id="notice"><%= notice %></p>
<article class="message is-dark">
  <div class="message-header">
    <p>Vendas</p>
  </div>
</article>  
<table class="table is-fullwidth is-striped">
  <thead>
    <tr>
      <th>Data</th>
      <th>Cliente</th>
      <th>Produto</th>
      <th colspan="3">#</th>
    </tr>
  </thead>

  <tbody>
    <% @products_clients.each do |pc| %>
      <tr>
        <td><%=l pc.date_sale %></td>
        <td><%= pc.client.name %></td>
        <td><%= pc.product.name %></td>
        <td><%= link_to 'Ver', pc ,class: "button is-small is-primary"%></td>
        <td><%= link_to 'Editar', edit_product_client_path(pc) ,class: "button is-small is-info"%></td>
        <td><%= link_to 'Deletar', pc,class: "button is-small is-danger ", method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'Nova Venda', new_product_client_path,class: "button is-link" %>
